# API base URL (inlined at build time). Relative paths in apiClient are resolved against this.
EXPO_PUBLIC_API_URL=

# Optional: endpoint used to refresh auth tokens (POST { refreshToken })
EXPO_PUBLIC_AUTH_REFRESH_URL=